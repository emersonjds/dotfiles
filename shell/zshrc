# ~/.zshrc — managed by dotfiles (copied, not symlinked).
# Edited the repo? run ./install.sh --configs-only. Edited this file? run ./sync.sh.
# Keeps PATH unique, including entries added later by nvm and friends.
typeset -U path PATH

export ZSH="$HOME/.oh-my-zsh"
ZSH_THEME=""   # prompt vem do starship
plugins=(git)
[ -r "$ZSH/oh-my-zsh.sh" ] && source "$ZSH/oh-my-zsh.sh"

# History melhorado
setopt HIST_IGNORE_ALL_DUPS HIST_IGNORE_SPACE SHARE_HISTORY INC_APPEND_HISTORY EXTENDED_HISTORY
setopt AUTO_CD AUTO_PUSHD PUSHD_IGNORE_DUPS

# Fragmentos de config. Ficam fora do repo de propósito: apagar o repo
# não pode quebrar o shell.
ZSH_CONFIG_DIR="${XDG_CONFIG_HOME:-$HOME/.config}/shell"
for f in env aliases plugins; do
  [ -r "$ZSH_CONFIG_DIR/$f.sh" ] && source "$ZSH_CONFIG_DIR/$f.sh"
done

# nvm (carregado tarde p/ não atrasar o shell). NVM_DIR vem do env.sh.
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
[ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"

# bun completions (BUN_INSTALL e PATH vêm do env.sh)
[ -s "$BUN_INSTALL/_bun" ] && source "$BUN_INSTALL/_bun"
