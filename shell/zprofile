# ~/.zprofile — gerenciado pelo dotfiles (cópia, não symlink).
# Só o que é de login shell. O resto do ambiente está em ~/.config/shell/env.sh.

# Homebrew: aqui e só aqui. O env.sh só chama de novo se o brew não estiver no PATH.
if [ -x /opt/homebrew/bin/brew ]; then
  eval "$(/opt/homebrew/bin/brew shellenv)"
elif [ -x /usr/local/bin/brew ]; then
  eval "$(/usr/local/bin/brew shellenv)"
elif [ -x /home/linuxbrew/.linuxbrew/bin/brew ]; then
  eval "$(/home/linuxbrew/.linuxbrew/bin/brew shellenv)"
fi
